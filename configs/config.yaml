defaults:
  - env: reach_env              # FIX: was 'franka_panda_default' which doesn't exist
  - reward: reward              # FIX: was 'dense_manipulation' which doesn't exist
  - algo: sac                   # default algo — override with algo=ppo or algo=td3
  - experiment: default_run
  - _self_

seed: 42
video_dump: false
wandb:
  project: "isaaclab_franka_suite"
  entity: "naganshul258-montanuniversit-t-leoben"          # replace with your W&B entity
  mode: "online"
